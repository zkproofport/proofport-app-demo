# Local preview using the deployed relay: set both values in .env.local.
# Gotgan's server-side adapter uses RELAY_URL; other demos use NEXT_PUBLIC_RELAY_URL.
NEXT_PUBLIC_RELAY_URL=https://relay.zkproofport.app
RELAY_URL=https://relay.zkproofport.app
