# Optional browser-side relay override for local previews using the deployed relay.
# Without this, localhost uses localhost:4001; production uses the production relay.
NEXT_PUBLIC_RELAY_URL=https://relay.zkproofport.app
